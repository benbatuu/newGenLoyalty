{
  "name": "@ngl/config",
  "version": "0.1.0",
  "private": true,
  "files": ["tsconfig.base.json"],
  "scripts": {
    "build": "echo \"config package — no build\"",
    "lint": "echo \"no lint yet\""
  }
}
